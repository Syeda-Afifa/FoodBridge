# Leave this unset for local development — the Vite proxy in vite.config.ts
# forwards /api to http://127.0.0.1:8000 already.
# Set it only when the deployed frontend talks to a backend on another domain.
# VITE_API_BASE_URL=https://your-backend-url.example.com/api
